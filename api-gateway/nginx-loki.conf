upstream loki-service {
  server fluent-bit:3100;
}

server {
  listen 3100;
  location / {
    auth_basic "Restricted Access";
    auth_basic_user_file /etc/nginx/.htpasswd;
    proxy_pass http://loki-service/;
  }
}
